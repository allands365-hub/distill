{
  "name": "reproduction",
  "description": "Generates reproduction steps, expected behavior, and observed behavior for a bug report.",
  "instruction": { "$file": "instruction.md" },
  "toolsets": [],
  "visibility": "POD",
  "agent_runtime": { "profile_id": "019f0ccf-773d-74e1-9ae6-1116c57d1134" },
  "input_schema": {
    "type": "object",
    "properties": {
      "title": { "type": "string" },
      "body": { "type": "string" }
    },
    "required": ["body"]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "repro_steps": { "type": "string" },
      "expected_behavior": { "type": "string" },
      "observed_behavior": { "type": "string" }
    },
    "required": ["repro_steps", "expected_behavior", "observed_behavior"]
  },
  "permissions": { "grants": [] }
}
